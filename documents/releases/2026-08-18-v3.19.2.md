# リリース記録: v3.19.2

- リリース日: 2026-08-18
- 対象プロジェクト: Masakai/meteo
- バージョン: v3.19.2
- 要件トレーサビリティ: なし（本番環境で発見された不具合。Issue・チケット番号の発行なし）
- 承認者: レビュー不要（CLAUDE.md 判断基準: バグ修正）
- 関連レビュー報告書: なし（レビュー不要カテゴリ）
- 関連実装仕様書: なし
- 関連設計書: なし

## リリース内容

### Fixed

- `detection_store.py` — UI で削除した検出が JSONL 再同期で復活する不具合を修正した。`detections.jsonl` は検出コアが追記し続ける一次記録であり削除時に行を消さない（`soft_delete()` は SQLite のみを更新する）が、`_insert_detection()` が同期時に `deleted` を `0` 固定で INSERT していたため、カメラ名変更（`migrate_camera_dirs.py`）などで `reset_sync_state()` による全行再同期が走ると、削除済みレコードが `deleted=0` で復活していた。復活したレコードは実ファイルが既に削除済みのため `image_path` が空文字列になり、ダッシュボードに「サムネイルが付かない検出」として並ぶ。本番環境では 2026-04-12〜2026-05-11 の期間で 4,875 件が影響を受けた。

### Added

- `detection_store.py` — 削除済み検出 ID を永続記録する `deleted_detections` テーブル（`id` / `camera` / `deleted_at`）を新設した。`soft_delete()` が削除履歴を登録し、`_insert_detection()` が `COALESCE((SELECT 1 FROM deleted_detections WHERE id = ?), 0)` で削除状態を復元するため、再同期や DB 再構築を経ても削除が取り消されない。テーブルは `CREATE TABLE IF NOT EXISTS` で既存 DB にも自動作成されるため、アップグレード時の手動マイグレーションは不要。
- `scripts/repair_lost_deletions.py` — 過去に失われた削除フラグを復旧するスクリプトを追加した。JSONL に画像パスが記録されているのに実ファイルが存在しないレコードを削除済みとみなして `deleted=1` に戻し、`deleted_detections` へ履歴を登録する。既定はドライランで、`--apply` を付けるまで DB を変更しない。
- `tests/test_detection_store.py` — 回帰テスト 4 件を追加した（削除履歴の記録、再同期後および DB 再構築後に復活しないこと、未削除レコードが残ること）。

### Changed

- `documents/DETECTION_STORE.md` / `documents/SCRIPTS_REFERENCE.md` — `deleted_detections` テーブルの仕様・背景と `repair_lost_deletions.py` の使い方を追記した。

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| バージョン番号更新 | v3.19.1 → v3.19.2（`dashboard_config.py`） | 完了 |
| CHANGELOG追記 | `## [3.19.2] - 2026-08-18`（Fixed / Added / Changed） | 完了 |
| ドキュメント更新 | `documents/SCRIPTS_REFERENCE.md` にサマリ行と詳細節を追加（新規スクリプト分） | 完了 |
| lint | `flake8 detection_store.py tests/test_detection_store.py scripts/repair_lost_deletions.py dashboard_config.py` エラーなし | 完了 |
| テスト | `pytest tests/test_detection_store.py -q` → 40 passed | 完了 |
| gitコミット | コミットハッシュ: `ee654e3`（7ファイル、pre-commit flake8 OK） | 完了 |
| タグ作成 | v3.19.2 | 完了 |
| プッシュ | origin/master、タグ v3.19.2 | 完了 |
| GitHub Release | https://github.com/Masakai/meteo/releases/tag/v3.19.2 | 完了 |

## 特記事項

### 本番環境（greeng4）のデータ復旧が未実施

本修正は再発を防止するが、**過去に復活済みの 4,875 件は自動修復されない**。本番環境では以下の手順が別途必要（本リリース作業では未実施）。

```bash
# コンテナ停止（SQLite 書き込み競合を避けるため必須）
./meteor-docker.sh stop

# ドライランで対象件数・月別内訳を確認
python scripts/repair_lost_deletions.py --detections-dir ~/meteo/detections

# 本実行
python scripts/repair_lost_deletions.py --detections-dir ~/meteo/detections --apply
```

greeng4 へのデプロイは Git 経由のみ（直接ファイル転送は行わない）。ダッシュボードは再ビルドが必要なため `./meteor-docker.sh rebuild` を使用する（数分かかるためバックグラウンド実行推奨）。

### レビュー要否の判断根拠

CLAUDE.md の「レビュー要否の判断基準」に照らし、本変更はバグ修正に該当するため reviewer を経由せず release-manager 直行とした。`deleted_detections` テーブルの新設は判断基準の「データ構造の変更」に触れうるが、(1) 既存テーブルのスキーマを変更しない追加のみの変更であること、(2) `CREATE TABLE IF NOT EXISTS` で後方互換に自動作成されること、(3) 目的が当該バグの修正に限定されていることから、レビュー必須の変更には当たらないと判断した。

### 既知の問題（本リリースとは無関係）

`tests/test_generate_compose.py::test_generate_compose_mask_path_failure` がローカル環境で 1 件失敗するが、これは未追跡の `masks/` 配下ファイル（`.gitignore` 対象）が存在することによる環境依存の失敗であり、本変更の前から発生している。clean worktree および CI では成功する。

### ロールバック手順

```bash
git revert ee654e3
```

`deleted_detections` テーブルはリバートしても残るが、参照されなくなるだけで既存動作に影響しない。ただしリバートすると削除レコードの復活が再発する。
