# リリース記録: v3.17.3

- リリース日: 2026-08-15
- 対象プロジェクト: meteo
- バージョン: v3.17.3
- 要件トレーサビリティ: 該当なし（本番機greeng4の運用中に観測された誤検知・多重検出事象への対応）
- 承認者: ユーザー（口頭指示、バグ修正のためCLAUDE.mdの判断基準によりreviewer不要）
- 関連レビュー報告書: 該当なし（バグ修正はレビュー不要区分）
- 関連実装仕様書: 該当なし
- 関連設計書: 該当なし

## リリース内容

### Changed
- `detection_state.py` — `stream_timeout` のデフォルト値を `10.0` 秒 → `30.0` 秒に変更。
- `meteor_detector_rtsp_web.py` — `STREAM_TIMEOUT` 環境変数で `state.stream_timeout` を上書き可能にした。

本番機greeng4（Intel N100, 4コア）で3カメラ同時稼働時、CPU使用率が各カメラコンテナ99%前後に達しており、まれにOpenCVの`cap.read()`が10秒以上ブロックされることがあった。ダッシュボードのカメラ監視ウォッチドッグ（`stream_alive`判定、`state.last_frame_time`が`stream_timeout`秒更新されないとストールとみなす）がこれを誤ってストリーム断と誤検知し、検出プロセスを強制再起動させていた。再起動の瞬間に内部バッファのフレームが一括で流れ込み、タイムスタンプが極端に圧縮された状態で処理されることで、1回のイベントで6件もの流星が誤って連続検出され、書き出されるMP4のfpsも実際のカメラ映像より大幅に速く（異常値に）なる副作用が生じていた。stream_timeoutを30秒に緩和することで、CPU飽和による一時的な遅延を誤検知しにくくする。

なお、恒久対策として処理解像度SCALEも0.7→0.5へ引き下げ済み（本リリースのコミットには含まれず、別途本番反映済みとの申し送りあり）。

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| バージョン番号更新 | v3.17.2 → v3.17.3（`dashboard_config.py`） | 完了 |
| CHANGELOG追記 | `## [3.17.3] - 2026-08-15` | 完了 |
| CONFIGURATION_GUIDE.md更新 | `STREAM_TIMEOUT` 環境変数をカメラコンテナの環境変数表に追記 | 完了 |
| lint確認 | flake8（変更3ファイル・pre-commitフック） | OK |
| テスト確認 | pytest 307 passed（ローカルの1件失敗は未追跡`masks/`ディレクトリ起因のローカル限定事象と判明、clean worktreeで再検証しパス確認済み） | OK |
| gitコミット | コミットハッシュ: `60d1d10` | 完了 |
| タグ作成 | v3.17.3 | 完了 |
| プッシュ | origin/master | 完了 |
| CI確認 | GitHub Actions `master CI` 全ジョブ成功（pip-audit / flake8 / pytest / bandit / docker build ×4） | 完了 |
| GitHub Release | https://github.com/Masakai/meteo/releases/tag/v3.17.3 | 完了 |

## 特記事項

- ローカル環境でのみ `tests/test_generate_compose.py::test_generate_compose_mask_path_failure` が失敗する事象を確認したが、原因は作業ディレクトリに残存する未追跡の `masks/camera1_mask.png`（`.gitignore` 対象）によるスキップ分岐であり、コード側の不具合ではない。`git worktree` で作成したクリーンな `HEAD` チェックアウトでは全テストがパスすることを確認済み。実際のCI（GitHub Actions）でも `Unit tests (pytest)` は成功しており、リリースブロッカーには該当しない。
- 本変更はダッシュボード監視ウォッチドッグの挙動を変更するのみで、APIエンドポイント・データフォーマットの変更はないため `documents/API_REFERENCE.md` の更新は不要と判断した。
- `STREAM_TIMEOUT` 環境変数は任意設定であり、未設定時は新デフォルト値（30秒）が使用される。既存の `docker-compose.yml` やデプロイ設定の変更は不要。
