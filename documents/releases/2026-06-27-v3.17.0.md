# リリース記録: v3.17.0

- リリース日: 2026-06-27
- 対象プロジェクト: Meteo (Meteor Event Tracking and Early Observation)
- バージョン: v3.17.0
- 要件トレーサビリティ: チケット番号なし（設計確定済み・指示書ベース）
- 承認者: reviewerエージェント（総評: 承認 / ブロッカーなし）
- 関連レビュー報告書: documents/reviews/2026-06-27-camera-mask-reset.md
- 関連実装仕様書: documents/specs/2026-06-27-camera-mask-reset.md
- 関連設計書: なし

## リリース内容

カメラ単位のマスクリセット機能を追加（新機能）。実行中の検出マスクを無効化し、保存済みマスク画像を削除する。

### Added
- `http_handlers.py` / `dashboard_camera_handlers.py` / `dashboard_routes.py` / `dashboard.py` — カメラ単位のマスクリセット機能（`POST /reset_mask` / `POST /camera_mask_reset/{index}`）。既存の `/update_mask` / `/confirm_mask` / `/discard_mask` 系と同じ4レイヤープロキシ構造・レスポンス形式を踏襲。
- プレビュー画面に「マスクリセット」ボタンを追加。
- `tests/test_http_handlers.py` — マスク削除ヘルパー（パストラバーサル防御含む）のテストを追加。
- `documents/API_REFERENCE.md` — `/reset_mask` / `/camera_mask_reset/{index}` のエンドポイント仕様を追記。

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| バージョン番号更新 | v3.16.0 → v3.17.0（dashboard_config.py） | 完了 |
| CHANGELOG追記 | ## [3.17.0] - 2026-06-27 | 完了 |
| flake8（pre-commit） | 変更pyファイル全てクリーン | 完了 |
| gitコミット | コミットハッシュ: 48591cb | 完了 |
| タグ作成 | v3.17.0（アノテートタグ） | 完了 |
| プッシュ | origin/master（commit + tag） | 完了 |
| GitHub Release | https://github.com/Masakai/meteo/releases/tag/v3.17.0 | 完了 |

## 特記事項

- コミット対象は指定の10ファイルのみ。作業ツリーに残る多数の未追跡ファイル（*.png スクリーンショット、`.coverage`、`detections.db`、`document.pdf`、`masks/`、`*.zip`、`documents/architecture_map.html`、`project_map.md`、`.playwright-mcp/` 等）はステージせず除外した。
- push 後に CI（flake8 / bandit / pytest / docker build / pip-audit）が走る。本タスク内では完了を待っていないため、CI 結果は別途追跡対象。
- レビュー報告書の申し送り: 全体テストの既存failure 2件（`test_create_app_go2rtc_asset_proxy`, `test_generate_compose_mask_path_failure`）は本変更と無関係（clean tree で再現確認済み）。リリースブロッカーではないが別途追跡を推奨。
- 本リリースはGitリポジトリへのリリースのみ。本番サーバー（greeng4）へのデプロイ・直接転送は未実施。
