# リリース記録: v3.16.0

- リリース日: 2026-06-19
- 対象プロジェクト: Meteo (meteor-event-tracking)
- バージョン: v3.16.0
- 要件トレーサビリティ: ユーザー要望（カメラ交換時のカメラ固有調整、口頭/チャット要件）
- 承認者: reviewer エージェント
- 関連レビュー報告書: documents/reviews/2026-06-19-per-camera-detection-settings.md
- 関連実装仕様書: documents/specs/2026-06-19-per-camera-detection-settings.md
- 関連設計書: documents/designs/2026-06-19-per-camera-detection-settings.md

## リリース内容

### Added
- `dashboard.py` / `dashboard_routes.py` — 検出設定を特定の1カメラのみへ適用する新規エンドポイント `POST /camera_settings/apply_one` を追加。リクエスト形式は `{camera, settings}` で、対象カメラは内部名または `camera_index` で指定する。既存の全カメラ一括適用 `apply_all` は無変更で温存（後方互換）。
- `dashboard_camera_handlers.py` — `handle_camera_settings_apply_one` を新設。対象カメラを線形解決し当該カメラのみへ設定を中継する。不正JSON・非object payload・カメラ解決不可・`settings` 非dict の各ケースは中継前に 400 を返し、対象外カメラへは到達しない。
- `dashboard_templates_settings.py` — `/settings` 画面に「対象カメラ」ドロップダウンと「現在値を読み込む」ボタンを追加。`__all__` 選択時は `apply_all`、個別カメラ選択時は `apply_one` を呼ぶ。カメラ表示名・内部名は `html.escape`（value は `quote=True`）でXSS対策済み。
- `tests/test_dashboard_routes.py` / `tests/test_dashboard_templates_settings.py` — `apply_one` の正常系・異常系、`current` の `process_min_dim`、ドロップダウン存在・XSSエスケープを検証するテストを追加。
- `documents/API_REFERENCE.md` / `documents/ARCHITECTURE.md` / `documents/CONFIGURATION_GUIDE.md` — 個別適用エンドポイント・設定反映2経路・対象カメラ選択の推奨手順を追記。

### Changed
- `dashboard_camera_handlers.py` — `GET /camera_settings/current` のレスポンス `results[i]` に `process_min_dim` を追加（既存フィールドは無変更）。これにより個別カメラ選択時に当該カメラの `process_min_dim` を基準に `exclude_edge_ratio` のpx換算ができるようになり精度が向上した。

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| バージョン番号更新 | v3.15.7 → v3.16.0（実装段階で更新済み、確認のみ） | 完了 |
| CHANGELOG追記 | ## [3.16.0] - 2026-06-19 | 完了 |
| gitコミット | コミットハッシュ: d78ecbe（14 files / +1075 -20） | 完了 |
| pre-commit (flake8) | OK | 完了 |
| タグ作成 | v3.16.0 | 完了 |
| プッシュ | origin/master（c74889d..d78ecbe）・タグ v3.16.0 | 完了 |
| GitHub Release | https://github.com/Masakai/meteo/releases/tag/v3.16.0 | 完了 |

## 特記事項

- push 前 flake8（変更7 .pyファイル）exit 0。dashboard系テスト 66 passed（実装/レビュー段階で確認済み）。
- 後方互換性: 全カメラ一括適用(apply_all)のハンドラ・ルート・payload形式・レスポンスは完全無変更。`current` への `process_min_dim` 追加は純粋な追加フィールド。
- レビュー Low 指摘2件（`camera_index` の bool 非除外、異常系テスト拡充）・Info 2件あり。いずれもリリースブロッカーではなく、UI実運用経路（内部名送信）では Low 指摘の事象は発生しない。次回の小改善で取り込む。
- 既存失敗 `test_generate_compose_mask_path_failure` は作業ツリーの `masks/*.png` 依存の環境問題で本変更と無関係。CI のクリーン環境では通る想定。
- 本番サーバ greeng4 へのデプロイは未実施（Git経由で別途実施）。本リリース作業範囲は push と GitHub Release まで。
- 手動E2E（実カメラで個別適用時に対象カメラのみ `runtime_settings/<camera>.json` 更新・他カメラ不変）は未実施。greeng4 以外の環境での確認を推奨。
